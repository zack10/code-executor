import { TerminalCleanPipe } from './terminal-clean-pipe';

describe('TerminalCleanPipe', () => {
  it('create an instance', () => {
    const pipe = new TerminalCleanPipe();
    expect(pipe).toBeTruthy();
  });
});
